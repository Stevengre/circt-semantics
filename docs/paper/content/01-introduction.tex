% 给读者关于本文工作的一个初步印象即可。
% circt 似乎也可以支持sv了https://discourse.llvm.org/t/is-circt-able-to-analyze-verilog/66281/22
\section{Introduction}

CIRCT~\ToCite{} (Circuit IR Compilers and Tools) has emerged as a significant infrastructure in the realm of hardware compilers and tools, influenced by the success of LLVM~\ToCite{} in software. It utilizes MLIR~\ToCite{} (Multi-Level Intermediate Representation) dialects to offer a flexible and interoperable framework, which has been increasingly adopted by new HDLs (Hardware Description Languages), including Chisel~\ToCite{}, Magma~\ToCite{}, and Moore~\ToCite{}, for compiler development. 

Unfortunately, CIRCT, devoid of formal semantics, introduces uncertainty and ambiguity, challenging its reliability in hardware design processes. This absence undermines the correctness and verifiability of the entire system, from input verification to output validation. As a result, the trustworthiness of hardware compilers, HDLs, and their dependent designs is compromised, marking a critical area for improvement.

\ToPreWork{Analysis of the Challenges and Related Work (Section~\ref{sec:related-work-challenges})}

\ToOpt{Our research addresses this critical issue by establishing the first formal semantics for CIRCT, aiming to provide a rigorous and reliable foundation for modern hardware design. We employ game semantics and effect handler for extensibility, provide Python API and command tools for accessibility, focus on the essence of CIRCT to cope with CIRCT's current rapid development in its early stage, employ validation-driven development to eliminate the ambiguity of the documentation, and establish a co-simulation framework for comprehensive validation of the semantics for correctness and completeness. Utilizing the $\mathbb{K}$ framework, Our contributions include:}

% from essence to extensibility, from accessibility to validation, from theory to implementation, from unit test to chip-level test, from formal semantics to co-simulation framework, from CIRCT to hardware design, from research to industry, from software to hardware, from LLVM to CIRCT, from HDL to CIRCT, from HDL to formal semantics, from HDL to co-simulation framework, from
\begin{enumerate}
    \item \ToWrite{(Essence) Theory for Extensibility and Essense for building upon and Early Stage. including the syntax of MLIR, the essence semantics of CIRCT (hardware semanics) and mechanisms for extensibility, such as effect handler or game semantics}  \ToCheck{We introduce K-CIRCT, an extensible and accessible framework for formalizing CIRCT dialects. Following a multi-layer approach, it provides the essence of the semantics to the extensible semantics step by step. It provides the core concepts and preprocesses of MLIR semantics, which is the foundation to formalize and compose the dialect semantics. With the framework, we can focus on the semantics of each dialect without concerning the relationship and consistency.}
    \item \ToWrite{(Extensibility) Implementation for extensibility and Test completeness of the essence. including the semantics of CIRCT' core dialect.} \ToCheck{We formalize the semantics of CIRCT core dialects, including HW, COMB, Seq, SV dialects. Since other CIRCT dialects can be compiled into the core dialects, this semantics is enough for real-world hardware (Rocket) simulation and verification.}
    \item \ToWrite{(Accessibility) Implementation for Accessible Executability. including kimulator and Python API for other applications} \ToCheck{We introduce a \textbf{C++ interface} to simulate the hardware, like verilator, based on the CIRCT semantics. }
    \item \ToWrite{(Ambiguity Elimination, Comprehensive Validation) Including the validation-driven development approach (observation), co-simulation(validation approach), validation conduction} \ToCheck{We propose a benchmark and co-simulation framework of CIRCT core dialects semantics. The benchmark includes the operation-level test cases for individual operations and the chip-level test cases for the whole semantics.}
\end{enumerate}

\ToWrite{roadmap. Section XXX}


\ToCheck{Accordingly, our goal is to establish the first formal semantics for CIRCT, thereby laying a rigorous and reliable foundation for contemporary hardware design. This endeavor is not only crucial for CIRCT's advancement but also brings forth a spectrum of challenges that must be meticulously addressed (Section~\ref{sec:related-work-challenges})}

\ToCheck{Specifically, we present the first formal semantics of the core dialects in CIRCT, including HW, Comb, Seq, and SV dialects, since they are the most stable representations in the infrastructure and other dialects can compile to them. We employ the $\mathbb{K}$ framework to construct the semantics and generate various tools for it. We first formalize the core MLIR, including the syntax, preprocess of operations, and the builtin dialect, to provide a solid and extendable foundation for the semantics of the dialects and the consistent composition of them. Then we formalize the semantics of the CIRCT core dialects, including HW, Comb, Seq, and SV dialects. With the previous foundation, we only need to focus on the distinctive features of each dialect. Finally, we propose a benchmark and co-simulation framework to end-to-end validate the semantics, by comparing the simulation result with verilator. The benchmark includes the semantic \textbf{unit test for each operation} in the dialects to help the development of formal semantics and validate the correctness of each operation. The benchmark also includes some famous hardware designs, including Rocket, BOOM, and Reiscinator, from https://github.com/circt/arc-tests to validate the effectiveness of the semantic. (Optional BOOM, Reiscinator, and tests from verilator)}

\ToCheck{Finally, we propose a semantic unit test for each operation in the dialects to validate the formal semantics and utilize the Rocket chip to validate that the core dialects are enough to represent real-world hardware.}
